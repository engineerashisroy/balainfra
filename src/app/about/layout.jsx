import React from "react";
export const metadata = {
  title: "About || About Bala Infra Constuction",
  description: "Details About Bala Infra Constrution",
};
const Layout = ({ children }) => {
  return <div>{children}</div>;
};

export default Layout;
